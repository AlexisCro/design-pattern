## 3.1
Dans l'implémentation actuelle de ce pattern on ne peut pas combiner plusieurs stratégies et donc les décorateurs ne sont pas empilables.
Il manque également une interface commune entre les types de café.
`Coffe` ne devrait pas être implémenté par les autres classes car chaque classe devrait être appelable indépendamment.

## 3.2
Voir le code
